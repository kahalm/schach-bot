services:
  schach-bot:
    build: /opt/stacks/schach-bot
    restart: unless-stopped
    env_file: /opt/stacks/schach-bot/.env
    volumes:
      - /opt/stacks/schach-bot/config:/app/config
      - /opt/stacks/schach-bot/books:/app/books
    mem_limit: 512m
    cpus: 0.5
